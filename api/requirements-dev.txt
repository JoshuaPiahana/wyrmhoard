# Testing and static analysis.
#
# Installed into the same image as everything else on purpose: one image that
# behaves identically on a laptop and in CI is worth more here than a lean
# one, and a check you need a second image to run is a check that stops being
# run.
pytest==9.1.1
httpx==0.28.1                 # FastAPI TestClient transport
ruff==0.16.5
mypy==2.3.1
yamllint==1.38.0
types-PyYAML==6.0.12.20260815
types-python-dateutil==2.9.0.20260807
