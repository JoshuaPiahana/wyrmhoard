# Everything, for the single image that runs every interface.
#
# The real dependency groups live in the files below, and pyproject.toml reads
# the same files, so pins are declared exactly once. If you only want the
# analysis engine, install requirements-core.txt on its own - it deliberately
# needs no web framework.
-r requirements-core.txt
-r requirements-cli.txt
-r requirements-web.txt
-r requirements-mcp.txt
-r requirements-dev.txt
