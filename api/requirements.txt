# Deliberately small. Every dependency here is one more thing that can rot in
# three years, and this tool needs to still run in three years.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pandas==2.2.3
PyYAML==6.0.2
Jinja2==3.1.5
python-dateutil==2.9.0.post0
pdfplumber==0.11.5
typer==0.15.1
# Pinned deliberately. typer 0.15 calls click's Parameter.make_metavar()
# without a ctx argument, which click 8.2 made mandatory - so an unpinned
# click silently breaks every CLI command with:
#   TypeError: make_metavar() missing 1 required positional argument: 'ctx'
# It only shows up on a fresh install, which is why an image built before
# click 8.2 kept working while a new clone did not.
click==8.1.8
rich==13.9.4

# Development tooling, kept in the same image on purpose. One image that
# behaves identically on your laptop and in CI is worth far more here than a
# lean one - and a check you need a second image to run is a check that
# stops being run.
pytest==8.3.4
httpx==0.28.1                 # FastAPI TestClient transport
ruff==0.8.6
mypy==1.14.1
yamllint==1.35.1
types-PyYAML==6.0.12.20241230
types-python-dateutil==2.9.0.20241206
